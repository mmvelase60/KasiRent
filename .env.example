# Leave unset for local embedded PostgreSQL. Set for a hosted PostgreSQL server.
# DATABASE_URL=postgresql://kasirent:password@localhost:5432/kasirent
PORT=3001
# On a physical phone, set mobile/.env EXPO_PUBLIC_API_URL to your computer's LAN URL.
